#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;

struct jugyou {
    vector<string> sd;
    vector<string> sb;
};

vector<vector<vector<jugyou>>> jikanwari;

int main()
{
    
    return 0;
}